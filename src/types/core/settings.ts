/**
 * Core Settings Types
 *
 * Claude Code Settings type definitions.
 * This re-exports from the fork's settings.ts to avoid duplication.
 *
 * Upstream canonical: src/types/core/settings.ts
 * Fork canonical: src/types/settings.ts (identical content)
 */

export * from "../settings";
