/**
 * Unified Preset Types (Derived)
 *
 * Consolidated preset type definitions for configuration management.
 * Combines settings presets, MCP presets, and unified presets into one coherent system.
 *
 * This re-exports from the fork's presets.ts to avoid duplication.
 * Upstream canonical: src/types/derived/preset.ts
 * Fork canonical: src/types/presets.ts (identical content)
 *
 * Location: ~/.claude-history-viewer/unified-presets/
 */

export * from "../presets";
